<IfModule mod_rewrite.c>
    RewriteEngine On

    # Eğer dosya/klasör doğrudan public/'in altındaysa, oradan servis et
    RewriteCond %{REQUEST_URI} !^.*/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
